# 网格生成工具 — VTU 读写与 `mesh-render` 渲染管线

> **待翻译（pending translation）**：本页为结构占位，完整内容见英文版
> [`docs/en-us/reference/mesh-render-and-vtu.md`](../../en-us/reference/mesh-render-and-vtu.md)。
> 摘要：本页涵盖契约 VTU 读写器（`src/io/vtu.rs`）、场景抽取层
> （`src/meshgen/render_scene.rs`，过滤器 + 调色板）、CPU 精确透明合成渲染器
> （`src/geometry/scene_render.rs`）以及 `mesh-render` 子命令
> （`src/pipeline/mesh_render.rs`、`src/config/mesh_render.rs`）。
